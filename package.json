{
  "name": "garnet",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:static": "npm run dev -w static",
    "dev:dynamic": "npm run dev -w dynamic",
    "install:all": "npm install --legacy-peer-deps"
  },
  "dependencies": {
    "@tanstack/react-query": "^5.90.21"
  }
}
